import React from "react";

import { Card } from "./Card";

export default {
  title: "Components/Card",
  component: Card,
};

const Template = () => (
  <Card text="Question " />
);
export const Primary = Template.bind({});
